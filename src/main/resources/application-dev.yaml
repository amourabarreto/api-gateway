spring:
  config:
    import: 'configserver:'
  cloud:
    config:
      discovery:
        service-id: ead-config-server
      username: ${USERNAME_CONFIG_SERVER}
      password: ${PASSWORD_CONFIG_SERVER}